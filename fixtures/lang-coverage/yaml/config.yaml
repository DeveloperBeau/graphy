name: graphy-yaml-fixture
version: "1.0.0"
description: YAML fixture for lang-coverage Tier 4

server:
  host: localhost
  port: 8080
  debug: true

database:
  host: localhost
  port: 5432
  name: graphy_db

logging:
  level: info
  format: json
