// feature: plain JS module imported by Svelte components
export const MAX_RETRIES = 3;
export const SERVICE_NAME = 'graphy-svelte-fixture';

export function formatName(name) {
    return 'hi, ' + name;
}
