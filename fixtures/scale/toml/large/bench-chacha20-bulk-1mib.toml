schema_rev = 3
suite = "cipher-matrix"

[run.chacha20_bulk_1mib]
primitive = "chacha20"
kind_of_primitive = "cipher"
workload = "bulk-1mib"
payload_bytes = 1048576
iterations = 2000
warmup_iterations = 20

[keying]
key_bits = 256
rotate_interval = 10000
zeroize_after = true

[recording]
database = "results/crypto.db"
table_name = "runs"
flush_every = 500
