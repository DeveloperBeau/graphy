"""Sample inputs for stream family checks."""


def corpus_stream():
    return [
        "running water",
        "wind from the north",
        "quiet before dawn",
    ]


def corpus_stream_size():
    return len(corpus_stream())
