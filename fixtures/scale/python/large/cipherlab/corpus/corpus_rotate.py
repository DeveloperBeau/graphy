"""Sample inputs for rotate family checks."""


def corpus_rotate():
    return [
        "turn the wheel",
        "round and round",
        "clockwise once more",
    ]


def corpus_rotate_size():
    return len(corpus_rotate())
