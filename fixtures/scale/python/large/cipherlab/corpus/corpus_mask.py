"""Sample inputs for mask family checks."""


def corpus_mask():
    return [
        "blue lantern lit",
        "signal from the tower",
        "keys in the garden",
    ]


def corpus_mask_size():
    return len(corpus_mask())
