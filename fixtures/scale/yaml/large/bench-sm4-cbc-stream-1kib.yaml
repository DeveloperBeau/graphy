defaults: &defaults
  harness: cryptobench
  warmup_iterations: 2000
  cool_down_secs: 2

sm4-cbc-stream-1kib:
  setup:
    <<: *defaults
    cipher: sm4-cbc
    key_bits: 256
    payload_bytes: 1024
  execute:
    <<: *defaults
    iterations: 200000
    cpu_pin: 2
  record:
    <<: *defaults
    database: results/crypto.db
    run_label: sm4-cbc-stream-1kib
