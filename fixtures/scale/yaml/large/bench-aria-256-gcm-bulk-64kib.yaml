defaults: &defaults
  harness: cryptobench
  warmup_iterations: 200
  cool_down_secs: 2

aria-256-gcm-bulk-64kib:
  setup:
    <<: *defaults
    cipher: aria-256-gcm
    key_bits: 256
    payload_bytes: 65536
  execute:
    <<: *defaults
    iterations: 20000
    cpu_pin: 2
  record:
    <<: *defaults
    database: results/crypto.db
    run_label: aria-256-gcm-bulk-64kib
