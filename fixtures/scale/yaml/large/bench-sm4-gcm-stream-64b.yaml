defaults: &defaults
  harness: cryptobench
  warmup_iterations: 5000
  cool_down_secs: 2

sm4-gcm-stream-64b:
  setup:
    <<: *defaults
    cipher: sm4-gcm
    key_bits: 256
    payload_bytes: 64
  execute:
    <<: *defaults
    iterations: 500000
    cpu_pin: 2
  record:
    <<: *defaults
    database: results/crypto.db
    run_label: sm4-gcm-stream-64b
