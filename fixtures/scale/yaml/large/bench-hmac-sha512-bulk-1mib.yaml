defaults: &defaults
  harness: cryptobench
  warmup_iterations: 20
  cool_down_secs: 2

hmac-sha512-bulk-1mib:
  setup:
    <<: *defaults
    cipher: hmac-sha512
    key_bits: 512
    payload_bytes: 1048576
  execute:
    <<: *defaults
    iterations: 2000
    cpu_pin: 2
  record:
    <<: *defaults
    database: results/crypto.db
    run_label: hmac-sha512-bulk-1mib
