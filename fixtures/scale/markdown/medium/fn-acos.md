# The acos function

Reference for `acos`, one of the built-ins listed in
[built-in functions](functions.md).

## acos usage

`acos(x)` evaluates immediately and lands on the tape like any other
entry. Argument errors raise `domain error`, described in
[error messages](error-messages.md).

## acos precision

Results carry full internal precision and round only at display time,
per [precision and rounding](precision.md).

Next function page: [atan](fn-atan.md).
