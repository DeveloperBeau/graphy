# Installation

Prebuilt binaries are published for Linux and macOS on every tagged
release. Download one, drop it on your PATH, and check it runs:

    inkroll --version

## Building from source

You need a C compiler and make. Clone the repository, then:

    make && make install

Once installed, head over to [everyday usage](usage.md) or return to the
[front page](index.md).
