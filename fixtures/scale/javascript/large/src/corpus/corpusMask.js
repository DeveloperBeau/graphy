// Sample inputs for mask family checks.
function corpusMask() {
  return [
    'blue lantern lit',
    'signal from the tower',
    'keys in the garden',
  ];
}

function corpusMaskSize() {
  return corpusMask().length;
}

export default { corpusMask, corpusMaskSize };
