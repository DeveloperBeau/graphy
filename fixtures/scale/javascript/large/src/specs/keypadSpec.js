// Test parameters for the keypad family.
function keypadSpec() {
  return { name: 'keypad', category: 'additive', key: 9 };
}

function keypadNotes() {
  return 'shift archetype, fixture key 9';
}

export default { keypadSpec, keypadNotes };
